apiVersion: v1
kind: Service
metadata:
  name: saas-starter-web
  namespace: workloads
  labels:
    app.kubernetes.io/name: saas-starter-web
    app.kubernetes.io/part-of: saas-starter
spec:
  selector:
    app.kubernetes.io/name: saas-starter-web
  ports:
    - name: http
      port: 80
      targetPort: http
